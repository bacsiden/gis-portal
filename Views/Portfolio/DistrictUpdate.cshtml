@model NationalIT.District
@{
    ViewBag.Title = "System config";
}
<h3>
    Cập nhật phường / xã</h3>
@*<div class="span12">*@
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
@using (Html.BeginForm(null, null, FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.ValidationSummary(true)                    

    @Html.HiddenFor(model => model.ID)
        
    <div class="control-group">
        <label class="control-label" for="Address">
            Quận / Huyện</label>
        <div class="controls">
            <div class="span12">
                @Html.TextBoxFor(model => model.Title, new { @class = "input-xlarge", Rows = "4" })
            </div>
        </div>
    </div>
    <div class="control-group">
        <label class="control-label" for="Address">
            Tỉnh / Thành phố</label>
        <div class="controls">
            <div class="span12">
                <select name="Province" class="chosen-with-diselect input-xlarge">
                    @Html.Raw(ViewBag.Province)
                </select>
            </div>
        </div>
    </div>
        
    <div class="form-actions">
        @Html.Partial("_ShowSaveEdit", "District")
    </div>
}
@*</div>*@