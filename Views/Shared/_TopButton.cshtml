@model string
@Html.ActionLink("Thêm mới", Model + "Update", null, new { @class = "btn btn-info addNewItem" })
<span>&nbsp;</span> <span>&nbsp;</span> @*@if ((bool)ViewBag.ShowButtonDelete)*@
@if (true)
{
    @Html.ActionLink("Xóa", Model + "Delete", null, new { @class = "btn btn-danger DeleteItem confirmDelete" })
}